# BioQuest 第三方域名白名单
# 来源：/workspace/index.html 实际出现的全部外部 URL（src/href），按用途分类。
# 规则：每行一个域名；空行与以 # 开头的行为注释，均忽略。
# 说明：本文件只列"允许出现"的域名，静态资源（script/link stylesheet/preload katex）
#       是否带 SRI 完整性由 scripts/verify-third-party.js 单独校验。

# ---- CDN 静态资源（css/js 加载；外链样式表/脚本必须带 SRI integrity）----
cdn.jsdelivr.net        # 主 CDN：KaTeX、vue 等；index.html 实际使用
cdnjs.cloudflare.com    # 备用 CDN（CSP style/font/img 允许，index.html 有 preconnect）

# ---- 交互式模拟 iframe 内嵌（仅 frame-src，不加载静态资源，无需 SRI）----
phet.colorado.edu

# ---- 脚本模块 CDN（CSP script-src 允许，当前 index.html 未直接引用，预留）----
unpkg.com

# ---- 本站部署域名（GitHub Pages 宿主，非第三方；canonical / og:image 指向它）----
astrnox.github.io

# ---- 外链导航链接（footer 外部站外链接，非加载的静态资源，无需 SRI）----
github.com
creativecommons.org